const assert=require("assert");process.env.NODE_ENV="test";process.env.MONGO_URI="mongodb://127.0.0.1/test";process.env.JWT_ACCESS_SECRET="phase16d-a";process.env.JWT_REFRESH_SECRET="phase16d-r";process.env.APP_ENCRYPTION_KEY="phase16d-encryption-key-with-32-characters";const{SettingDefinitionRegistry}=require("../src/services/settings/SettingDefinitionRegistry");const{registerPaymentSettings,defs}=require("../src/services/settings/PaymentSettingDefinitions");const Encryption=require("../src/services/settings/SettingsEncryptionProvider");(async()=>{const r=registerPaymentSettings(new SettingDefinitionRegistry());assert.strictEqual(r.getGroup("payments").length,defs.length);for(const k of ["payments.stripe.secretKey","payments.stripe.webhookSecret","payments.wise.apiToken","payments.hbl.password","payments.hbl.integritySalt"])assert.ok(r.get(k).encrypted);const e=new Encryption(),p=e.encrypt("secret");assert.strictEqual(e.decrypt(p),"secret");assert.ok(!JSON.stringify(p).includes("secret"));const routes=require("../src/routes/adminSettings").stack.filter(x=>x.route).map(x=>x.route.path);for(const pth of ["/payments","/payments/:provider","/payments/:provider/secrets/:key","/payments/:provider/test","/payments-health","/payments-status"])assert.ok(routes.includes(pth));console.log("PASS Phase 16D Payment Center tests");})().catch(e=>{console.error(e);process.exitCode=1;});

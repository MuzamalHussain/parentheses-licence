const assert=require("assert");process.env.NODE_ENV="test";process.env.MONGO_URI="mongodb://127.0.0.1/test";process.env.JWT_ACCESS_SECRET="phase16e-a";process.env.JWT_REFRESH_SECRET="phase16e-r";process.env.APP_ENCRYPTION_KEY="phase16e-encryption-key-with-32-characters";const{SettingDefinitionRegistry}=require("../src/services/settings/SettingDefinitionRegistry");const{registerAIProviderSettings,PROVIDERS}=require("../src/services/settings/AIProviderSettingDefinitions");const Encryption=require("../src/services/settings/SettingsEncryptionProvider");const{classify}=require("../src/services/AIProvidersCenterService");(async()=>{const r=registerAIProviderSettings(new SettingDefinitionRegistry());assert.strictEqual(Object.keys(PROVIDERS).length,9);assert.strictEqual(r.getGroup("ai").length,45);for(const id of Object.keys(PROVIDERS)){assert.ok(r.get(`ai.${id}.apiKey`).encrypted);assert.ok(Array.isArray(PROVIDERS[id].caps));}const e=new Encryption(),p=e.encrypt("ai-secret");assert.strictEqual(e.decrypt(p),"ai-secret");assert.ok(!JSON.stringify(p).includes("ai-secret"));assert.strictEqual(classify({response:{status:401}}),"authentication_failed");assert.strictEqual(classify({response:{status:429,data:{error:{message:"quota exceeded"}}}}),"quota_exceeded");assert.strictEqual(classify({response:{status:404}}),"model_not_found");assert.strictEqual(classify({code:"ETIMEDOUT"}),"timeout");const routes=require("../src/routes/adminSettings").stack.filter(x=>x.route).map(x=>x.route.path);for(const pth of ["/ai-providers","/ai-providers/:id","/ai-providers/:id/secrets/:key","/ai-providers/:id/test","/ai-providers-health","/ai-providers-capabilities","/ai-providers-status"])assert.ok(routes.includes(pth));console.log("PASS Phase 16E AI Providers Center tests");})().catch(e=>{console.error(e);process.exitCode=1;});

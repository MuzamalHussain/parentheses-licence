const mongoose = require("mongoose"); const schema = new mongoose.Schema({ event: { type: String, required: true, match: /^(setting|general|email|payment|ai|storage|security|password|jwt|session|rate_limit|cors|ip|feature|maintenance|kill_switch|health|provider|configuration)\.[a-z._]+$/ }, key: { type: String, default: "", index: true }, group: { type: String, default: "", index: true }, actorId: { type: mongoose.Schema.Types.ObjectId, ref: "User", default: null }, requestId: { type: String, default: "" }, ip: { type: String, default: "" }, version: { type: Number, default: null }, metadata: { type: mongoose.Schema.Types.Mixed, default: {} } }, { timestamps: true, collection: "runtime_setting_audits" }); schema.index({ createdAt: -1 }); module.exports = mongoose.model("RuntimeSettingAudit", schema);

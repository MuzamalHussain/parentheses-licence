const asyncHandler = require("express-async-handler"); const manager = require("../services/configuration/ConfigurationManager");
const context = (req) => ({ actorId: req.user._id, requestId: req.id, ip: req.ip }); const list = (value) => value ? String(value).split(",").map((x) => x.trim()).filter(Boolean) : undefined;
exports.overview = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.overview() })); exports.snapshots = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.list(req.query) }));
exports.backup = asyncHandler(async (req, res) => res.status(201).json({ success: true, message: "Configuration backup created.", data: await manager.backup(req.body, context(req)) }));
exports.restore = asyncHandler(async (req, res) => res.json({ success: true, message: req.body.preview ? "Restore preview generated." : "Configuration restored.", data: await manager.restore(req.body.snapshotId, req.body, context(req)) }));
exports.import = asyncHandler(async (req, res) => res.json({ success: true, message: req.body.preview ? "Import preview generated." : "Configuration imported.", data: await manager.import(req.body.payload, req.body, context(req)) }));
exports.export = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.export({ groups: list(req.query.groups), keys: list(req.query.keys), encrypted: req.query.encrypted !== "false", includeSecrets: req.query.includeSecrets === "true" }, context(req)) }));
exports.versions = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.versions(req.query) })); exports.diff = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.diff({ left: req.query.left || "current", right: req.query.right }) }));
exports.rollback = asyncHandler(async (req, res) => res.json({ success: true, message: req.body.preview ? "Rollback preview generated." : "Configuration rolled back.", data: await manager.rollback(req.body.snapshotId, req.body, context(req)) }));
exports.validate = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.verify(req.body.snapshotId) })); exports.recover = asyncHandler(async (req, res) => res.json({ success: true, message: "Configuration recovery completed.", data: await manager.recover(req.body.action, req.body, context(req)) }));
exports.remove = asyncHandler(async (req, res) => { await manager.remove(req.params.id, context(req)); res.json({ success: true, message: "Configuration snapshot deleted." }); }); exports.cleanup = asyncHandler(async (req, res) => res.json({ success: true, data: await manager.cleanup(context(req)) }));

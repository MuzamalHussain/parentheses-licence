const express = require("express"); const { requireAuth, requireRole } = require("../middleware/auth"); const c = require("../controllers/configurationManagementController"); const router = express.Router(); router.use(requireAuth, requireRole("admin"));
router.get("/overview", c.overview); router.get("/snapshots", c.snapshots); router.post("/backup", c.backup); router.post("/restore", c.restore); router.post("/import", c.import); router.get("/export", c.export); router.get("/versions", c.versions); router.get("/diff", c.diff); router.post("/rollback", c.rollback); router.post("/validate", c.validate); router.post("/recovery", c.recover); router.delete("/snapshots/:id", c.remove); router.post("/retention/cleanup", c.cleanup); module.exports = router;

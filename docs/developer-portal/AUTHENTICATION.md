# Authentication

Use bearer API keys:

```http
Authorization: Bearer pl_live_or_pl_test_key
```

Production keys begin with `pl_live`. Sandbox keys begin with `pl_test`.

Required scopes are documented per endpoint.
